---
name: "个人主页-文章编写-蓝队文章编写"
description: "x7peeps.github.io 蓝队安全文章编写技能。涵盖取证分析深度文章、漏洞分析专题、勒索组织情报等蓝队内容的规划、撰写与验证全流程。当用户要求编写安全文章、新增蓝队内容、规划文章批次、或提到 x7peeps 文章编写时调用此技能。"
---

# 个人主页 - 蓝队文章编写

本技能定义了 x7peeps.github.io Hugo 安全博客中蓝队方向文章的完整编写规范，涵盖两大内容线：**取证分析深度文章**和**漏洞分析专题文章**。基于 100+ 篇文章的实战经验沉淀而成。

---

## 一、站点结构

### Hugo 项目根目录
```
hugo-src/
  content/
    安全/
      应急响应/
        0x03取证分析/          ← 取证分析深度文章
          内存取证分析/
          恶意样本分析/
          页面篡改分析/
          系统入侵对抗研究/
          全链路取证分析/
          _index.md
      渗透测试/
        03 漏洞分析/           ← 漏洞分析专题文章
          _index.md
          常见高危RCE产品线清单.md  ← 覆盖清单（需同步更新）
          边界设备与安全网关/
          企业应用与CMS/
          ...（30+ 子目录）
```

### 文章存放规则
| 文章类型 | 存放路径 | 命名规范 |
|---------|---------|---------|
| 取证分析深度文章 | `hugo-src/content/安全/应急响应/0x03取证分析/` | `{主题名}.md`（中英文混合，无空格） |
| 漏洞分析专题文章 | `hugo-src/content/安全/渗透测试/03 漏洞分析/{产品线目录}/` | `_index.md`（作为产品线入口） |
| 覆盖清单更新 | `hugo-src/content/安全/渗透测试/03 漏洞分析/常见高危RCE产品线清单.md` | 追加新条目 |

---

## 二、取证分析深度文章规范

### 2.1 基本要求
- **字符数**：≥ 20,000（用 `wc -m` 验证）
- **章节数**：10+ 章（0x01 到 0x0A 或更多）
- **面向读者**：专业安全人员 / 应急响应人员，内容面向成人读者
- **代码注释**：不要添加任何代码注释

### 2.2 Hugo Front Matter 格式

```yaml
---
title: "{中文标题}"
date: {YYYY-MM-DD}T{HH:MM:SS}+08:00
draft: false
weight: {递增值}
description: "{一句话描述，覆盖所有关键词}"
categories: ["应急响应", "取证分析"]
tags: ["{标签1}", "{标签2}", ... "{标签N}"]
---
```

**weight 递增规则**：每篇文章 weight 递增 10，当前序列：
- 360: 恶意软件行为分析 → 370: WMI持久化 → ... → 540: 工控系统
- 下一篇应为 **550**

**description 规范**：用一句话概括全文核心内容，用中文逗号分隔各个技术点，字数 80-150 字。

**tags 规范**：8-10 个标签，中英文混合，覆盖技术点、工具名、攻击类型、平台名。

### 2.3 章节结构模板

每篇文章必须包含以下标准章节（可根据主题增减）：

| 章节编号 | 必选/可选 | 内容 |
|---------|----------|------|
| 0x01 | **必选** | 技术基础与取证概述（架构、与传统取证差异、工具链） |
| 0x02-0x0N | **必选** | 核心技术章节（每个技术点独立成章，深入展开） |
| 0x0N+1 | **必选** | 证据强度分层与案例关联 |
| 0x0N+2 | **必选** | 自动化检测与狩猎（Sigma/Bash/Python/YARA） |
| 0x0N+3 | **必选** | 公开案例分析（≥ 2 个案例） |
| 最后一章 | **必选** | 参考资料（≥ 8 条，含 URL） |

### 2.4 内容要素（每篇必含）

#### A. 证据强度分层
每篇文章必须包含证据强度分类章节，使用三级分类：
- **🔴 确认恶意**：有明确恶意意图和行为的证据
- **🟡 高度可疑**：强烈暗示恶意活动但需进一步验证
- **🟢 需要关注**：可能为正常行为但需结合上下文判断

#### B. 自动化检测脚本
每篇至少包含以下类型的脚本/规则（各至少 1 条）：
- **Sigma YAML 规则**：适用于 SIEM 平台的日志检测规则
- **Bash 脚本**：Linux/macOS 环境下的自动化狩猎脚本
- **Python 脚本**：数据分析、日志解析、自动化检测
- **YARA 规则**（可选）：恶意软件特征匹配

#### C. 公开案例分析
- ≥ 2 个真实 APT 组织或公开攻击事件案例
- 每个案例包含：攻击链描述、取证发现、IOC、经验教训
- 案例来源：公开威胁情报报告、安全厂商分析报告

#### D. 参考资料
- ≥ 8 条参考资料
- 包含 URL 链接
- 覆盖：官方文档、安全研究论文、开源工具文档、威胁情报报告

### 2.5 写作风格规范
- 中文撰写，**技术术语保留英文**（如：Process Hollowing、DLL Injection、LOLBin）
- 每个技术点都要有具体的**命令/代码示例**
- 使用**表格对比分析**（至少每章 1 个表格）
- 使用**代码块**展示命令、脚本、配置
- 使用 **MITRE ATT&CK 技术编号**标注攻击技术（如 T1562.001）
- 使用**层级标题**组织内容（h2 章节标题 + h3 小节标题）

### 2.6 已完成文章选题参考

以下主题已有深度文章，**不应重复编写**：
- WMI持久化、DLL劫持、令牌操纵、LOLBins、AMSI/ETW、Kerberos攻击
- NTFS ADS、Windows Defender/EDR对抗、容器/K8s取证、云环境(AWS/Azure/GCP)
- 邮件系统、浏览器、Linux系统、社会工程学、网络协议滥用、内存取证(Volatility)
- 移动设备(iOS/Android)、macOS系统、工控系统(ICS/SCADA)
- PowerShell攻击链、Windows认证机制、AD持久化、进程注入、反取证技术
- 恶意软件行为分析与沙箱检测

### 2.7 推荐的未覆盖选题方向
- 供应链攻击取证（软件供应链/硬件供应链/依赖投毒）
- 物联网(IoT)取证（智能家居/IoT网关/固件分析）
- 无线网络取证（WiFi攻击流量/蓝牙取证/SDR分析）
- 区块链与加密货币取证（链上追踪/钱包取证/DeFi攻击）
- 数据库深度取证（MySQL/PostgreSQL/Redis/MongoDB 注入痕迹）
- 蓝队检测工程（检测规则开发流程/误报管理/检测覆盖度评估）
- 数字签名与信任链取证（代码签名/PKI攻击/证书透明度）
- 虚拟化与云原生取证（KVM/Hypervisor/containerd/Envoy/Istio）

---

## 三、漏洞分析专题文章规范

### 3.1 基本要求
- **字符数**：≥ 8,000（产品线专题，用 `wc -m` 验证）
- **覆盖漏洞数**：每篇至少覆盖 3-6 个 CVE/漏洞
- **面向读者**：渗透测试人员 / 安全研究员

### 3.2 Hugo Front Matter 格式

```yaml
---
title: "{产品线名称}高危攻击链专题"
date: {YYYY-MM-DD}T{HH:MM:SS}+08:00
draft: false
weight: {按目录内已有 weight 最大值 +10}
description: "围绕 {产品线} 的高危漏洞攻击链专题分析..."
categories: ["渗透测试", "漏洞分析"]
tags: ["{产品线}", "{CVE编号}", ...]
---
```

### 3.3 章节结构模板

| 章节 | 内容 |
|------|------|
| 0x01 | 产品概述与攻击面分析（架构、部署方式、常见配置问题） |
| 0x02-0x0N | 单个 CVE/漏洞深度分析（每个漏洞独立成章） |
| 0x0N+1 | 攻击链组合利用（多漏洞串联利用场景） |
| 0x0N+2 | 自动化检测（Nuclei YAML / Python PoC / Sigma） |
| 0x0N+3 | 防御建议与修复方案 |
| 最后一章 | 参考资料（≥ 6 条） |

### 3.4 每个 CVE 章节标准内容

```markdown
## 0x0N CVE-XXXX-XXXXX 漏洞名称

### 漏洞概述
- 影响产品与版本
- 漏洞类型（CWE 分类）
- CVSS 评分
- 利用条件

### 技术原理
- 根因分析
- 攻击面定位
- 代码层面漏洞（如适用）

### 复现与 PoC
（HTTP 请求 PoC / Python 脚本 / Nuclei 模板）

### 检测与狩猎
（Sigma 规则 / 日志特征 / 流量特征）

### 修复建议
- 官方补丁
- 临时缓解措施
- 配置加固

### 参考链接
```

### 3.5 PoC 代码规范
- HTTP 请求使用原始格式（不依赖特定工具）
- Python 脚本使用 `requests` 库（项目已使用）
- Nuclei YAML 模板遵循标准格式
- 所有 PoC 仅用于**授权测试**

### 3.6 覆盖清单维护
每新增一个产品线专题后，必须同步更新 `常见高危RCE产品线清单.md`，追加新条目。

---

## 四、Spec 驱动开发流程

### 4.1 批次规划

每次编写文章采用**批次制**，每批 3 篇文章（并行编写提高效率）：

```
.trae/specs/
  forensics-articles-batch-{N}/
    spec.md      ← 规划文档（为什么写、写什么、验收标准）
    tasks.md     ← 任务列表（可勾选的子任务）
    checklist.md ← 验证清单（字符数、构建、内容要素）
```

### 4.2 选题流程
1. 列出已有文章，排除重复主题
2. 选择 3 个尚未覆盖的高价值领域
3. 创建 spec 目录和文件
4. 并行启动子代理编写

### 4.3 编写流程
1. 创建 spec 文档（spec.md / tasks.md / checklist.md）
2. 并行启动 3 个子代理，每个负责 1 篇文章
3. 每个子代理完成后用 `wc -m` 验证字符数
4. 若字符不足 20,000，子代理自行扩充直到达标
5. 运行 `hugo --minify` 验证构建通过
6. 更新 tasks.md（勾选已完成任务）和 checklist.md

### 4.4 验证检查点
- [ ] 文件已创建且字符数 ≥ 要求
- [ ] Hugo 构建通过（无报错）
- [ ] 包含证据强度分层章节
- [ ] 包含自动化检测脚本
- [ ] 包含公开案例分析（≥ 2 个）
- [ ] 包含参考资料（≥ 8 条）

---

## 五、Hugo 构建注意事项

### 5.1 已知问题
- AI 目录下的 `hint` shortcode 缺失会导致全局构建报错，这是**预存在的问题**，与新文章无关
- 新文章验证时，只要文章所在页面能正常构建即可
- 构建命令：`cd hugo-src && hugo --minify`

### 5.2 构建验证方法
```bash
cd hugo-src && hugo --minify 2>&1 | tail -15
```
- 确认 `Total in X ms` 输出且无 ERROR
- WARN 级别的 deprecated 提示可忽略

### 5.3 字符数验证
```bash
wc -m "hugo-src/content/安全/应急响应/0x03取证分析/{文件名}.md"
```

---

## 六、质量标准总结

| 维度 | 取证分析深度文章 | 漏洞分析专题文章 |
|------|----------------|----------------|
| 最低字符数 | 20,000 | 8,000 |
| 章节数 | 10+ | 根据 CVE 数量 |
| 代码脚本 | Sigma + Bash + Python | Nuclei + Python + Sigma |
| 案例数 | ≥ 2 个 APT/攻击事件 | ≥ 3 个 CVE 详解 |
| 参考资料 | ≥ 8 条 | ≥ 6 条 |
| 证据分层 | 三级分类 | 漏洞严重性分级 |
| MITRE ATT&CK | 必须标注 | 建议标注 |
